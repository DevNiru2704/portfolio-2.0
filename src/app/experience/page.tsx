import PortfolioApp from "@/components/shared/PortfolioApp";
import { experienceMetadata } from "@/lib/metadata";

export const metadata = experienceMetadata;

export default function ExperiencePage() {
  return <PortfolioApp />;
}